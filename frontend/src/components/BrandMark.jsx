import { Gavel } from "lucide-react";

// The JurisAI logo glyph (a gavel) used in the brand lockups.
export default function BrandMark({ size = 20 }) {
  return <Gavel size={size} strokeWidth={2.2} />;
}
