# --- Core web framework ---
Django>=5.0,<6.1
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
python-dotenv>=1.0

# --- Document parsing ---
PyMuPDF>=1.24          # PDF text extraction (imported as `fitz`)
python-docx>=1.1       # DOCX reading/writing
reportlab>=4.1         # PDF generation for downloads

# --- Legal Advisor web retrieval (optional, graceful fallback if offline) ---
requests>=2.31
beautifulsoup4>=4.12

# --- LLM provider: Google Gemini (free tier) - default ---
# Get a free key at https://aistudio.google.com/apikey
google-genai>=0.3

# --- Optional alternative LLM providers (only needed if you use them) ---
# The app runs without these via offline fallbacks.
openai>=1.30
anthropic>=0.40

# --- Optional production database drivers (uncomment what you need) ---
# psycopg2-binary>=2.9      # PostgreSQL
# mysqlclient>=2.2          # MySQL
